{
  "name": "vue-front-blog",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt --port 8084",
    "pm2": "pm2 start ./pm2.json",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "ssrDev": "cross-env NODE_ENV=development node server/app.js --watch server --mode development",
    "ssrDev:pro": "cross-env NODE_ENV=development nodemon server/app.js --watch server --mode production",
    "ssrBuild": "nuxt build --mode production",
    "ssrStart": "cross-env NODE_ENV=production node server/app.js"
  },
  "dependencies": {
    "@nuxtjs/axios": "^5.12.2",
    "core-js": "^3.6.5",
    "dayjs": "^1.9.1",
    "element-ui": "^2.13.2",
    "express": "^4.17.1",
    "nuxt": "^2.14.5"
  },
  "devDependencies": {
    "cross-env": "^7.0.2",
    "node-sass": "^4.14.1",
    "nodemon": "^2.0.6",
    "sass-loader": "^10.0.2"
  }
}
